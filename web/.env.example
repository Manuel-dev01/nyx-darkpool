# Origin of the Go matching engine's HTTP API (cmd/server, default :8080).
# The Next.js server proxies /api/engine/* to this origin (see next.config.mjs),
# so the browser never talks to the engine directly and there is no CORS setup.
# Copy this file to .env.local and adjust if the engine runs elsewhere.
ENGINE_ORIGIN=http://localhost:8080
