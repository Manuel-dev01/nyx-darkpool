import type { Metadata } from "next";
import { ProofsBody } from "../../_components/ProofsBody";

export const metadata: Metadata = { title: "Nyx · Proofs" };

export default function Proofs() {
  return <ProofsBody />;
}
